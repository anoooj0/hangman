rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all read/write access for debugging
    // This is very permissive and should only be used for development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
